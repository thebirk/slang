var d = lol;
var a = 123;
var b = 32 + (3 * a);

fn main(args) {
    var a = 123;
    var b = 321*test(a[i]);

    a = 32;
    b += 123;
    b -= a;

    if a {
        a += 1;
    }

    var c = 32 + (2 * 4);
    var d = null;

    if b == true do a -= 1;

    println(a);
    println(b);

    {
        var a = 123;
    }
    do print();

    return 123;
}